package com.app.shivam.config;


//@Configuration
//@EnableSwagger2
public class SwaggerConfig {
	/*@Bean
	public Docket createDocket() {
		return new Docket(DocumentationType.SWAGGER_2)
				.select()
				.apis(RequestHandlerSelectors.basePackage("com.app.shivam.rest"))
				//.paths(PathSelectors.any())
				.paths(PathSelectors.regex("/v1/api.*"))
				.build()
				.pathMapping("/")
				.apiInfo(apiInfo());
	}

	private ApiInfo apiInfo() {
		return new ApiInfo("STUDENT APP", 
				"CRUD APP", 
				"3.2GA", 
				"http://sample.com", 
				new Contact("SHIVAM", "http://github.com/shivamvishwakarma150", "shivam.972176@gmail.com"), 
				"APACHE", 
				"http://apache.com", 
				Collections.emptyList());
	}*/
}
